import * as React from 'react';
import Button from '@mui/material/Button';

export default function App() {
  return <Button variant="contained">Hello World, V3! We are now hosted on a custom domain!</Button>;
}
